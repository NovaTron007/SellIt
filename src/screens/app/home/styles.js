import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 24
    },
    title: {
        fontSize: 16
    },
    flatList: {
        paddingVertical: 24
    }
})

export default styles