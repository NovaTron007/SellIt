import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        marginVertical: 24,
    },
    flatList: {
        marginVertical: 8
    }
})

export default styles